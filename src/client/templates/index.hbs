{{!-- layouts/base.hbs --}}
<!doctype html>
<html class="no-js" lang="de">
	<head>
		<meta charset="utf-8" />
		<title>{{#if title}}{{title}}{{else}}Jan Feldmann{{/if}}</title>

		{{!-- Custom <head> content per page could be added. --}}
		{{#> head-block}}
		{{!--
		The `includes/head` partial is the default content,
		but can be overridden.
		--}}
		{{> partials/includes/head }}
		{{/head-block}}
	</head>
	<body>
        {{!-- Custom service content goes here. --}}
        {{#> services-block}}
            {{!--
            The `components/browsehappy` and  `components/noscript` partial are
            the default content, but can be overridden.
            --}}
            {{> components/browsehappy/browsehappy }}
            {{> components/noscript/noscript }}
        {{/services-block}}

		<div id="page-wrapper">
			{{!-- Main content goes here. --}}
			{{> components/header/header }}
        <section>
            {{> components/stage/stage }} 
        </section>
        <main class="main">
            <section class="section">
                <div class="container">
                    <header> 
                        <h2>Warum Frontend?</h2>
                    </header>
                    <p>Als Brücke zwischen Technik und Design bietet mir das Frontend die Möglichkeit meine Ideen visuell und in der Programmierung zum Ausdruck zu bringen. Es ist ständig in Bewegung und entwickelt sich weiter.</p>
                    <p><strong>&rarr; Im Frontend wird es nie langweilig</strong></p>
                </div>
            </section>
            <section class="section">
                <div class="container">
                    <header>
                        <h2>Erfahrung</h2>
                    </header>
                    <p>Seit meinen ersten Code-Zeilen habe ich zahlreiche Projekte geleitet und erfolgreich fertiggestellt.</p>
                </div>
            </section>
            {{!--
            <section class="section">
                <div class="container">
                    {{> slider }}
                </div>
            </section>
            --}}
        </main>

			{{!-- Custom <footer> content per page could be added. --}}
			{{#> footer-block}}
			{{!--
			The `includes/footer` partial is the default content,
			but can be overridden.
			--}}
			{{> components/footer/footer }}
			{{/footer-block}}

			{{!-- Custom scripts per page can be added. --}}
			{{#> scripts-block}}
			{{!--
			The `includes/jsfooter` partial is the default content,
			but can be overridden.
			--}}
			{{/scripts-block}}
		</div>
	</body>
</html>
